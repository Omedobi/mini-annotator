# Model-Assisted Annotation

- `make setup` → scaffold folders/configs  
- `make annotate` → launch Streamlit annotator  
- `make qa` → run sanity checks  
- `make export-coco` / `make export-yolo` → produce training datasets

**Ground truth:** `annotations.jsonl`