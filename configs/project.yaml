app:
  image_root: data/images
  top_k: 3
  policy: uncertain
classifier:
  model_name: ViT-B-32
  pretrained: openai
  device: auto
detector:
  enabled: true
  weights: yolov8n.pt   # fallback: rtdetr-l.pt
  conf: 0.25
  iou: 0.6
  max_det: 200
ui:
  max_image_side: 1024
